{% extends 'base.html.twig' %}

{% block title %}Профиль{% endblock %}

{% block body %}
    <div class="container">
        <main>
            <div class="py-5 text-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="60" height="60" fill="currentColor" class="bi bi-person-badge" viewBox="0 0 16 16">
                    <path d="M6.5 2a.5.5 0 0 0 0 1h3a.5.5 0 0 0 0-1zM11 8a3 3 0 1 1-6 0 3 3 0 0 1 6 0"/>
                    <path d="M4.5 0A2.5 2.5 0 0 0 2 2.5V14a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V2.5A2.5 2.5 0 0 0 11.5 0zM3 2.5A1.5 1.5 0 0 1 4.5 1h7A1.5 1.5 0 0 1 13 2.5v10.795a4.2 4.2 0 0 0-.776-.492C11.392 12.387 10.063 12 8 12s-3.392.387-4.224.803a4.2 4.2 0 0 0-.776.492z"/>
                </svg>
                <h2 class = "py-2">Профиль {{ app.user.userName }}</h2>
                <p class="lead">Привет, {{ app.user.userName }}! Ниже ты можешь увидеть все данные, которые ты вводил при регистрации и которые система присвоила тебе автоматически.</p>
            </div>

            <div class="g-5 row justify-content-center">
                <div class="col-md-7 col-lg-8">
                    <h4 class="mb-3">Информация</h4>
                        <div class="row g-3 justify-content-center">
                            <div class="col-12">
                                <p class="form-label">{{ app.user.userName }}</p>
                            </div>
                            <div class="col-12">
                                <p class="form-label">{{ app.user.email }}</p>
                            </div>
                            <div class="col-12">
                                <p class="form-label">{{ app.user.roles[0] }}</p>
                            </div>
                            <div class="col-12">
                                <p class="form-label">{{ app.user.isVerified }}</p>
                            </div>
                        </div>
                </div>
            </div>
        </main>
    </div>
{% endblock %}
